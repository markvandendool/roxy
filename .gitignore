# Virtual environment
venv/
__pycache__/
*.pyc
*.pyo

# Database files (regeneratable)
chroma_db/
*.db

# Logs and temp files
*.log
*.tmp
.DS_Store

# Model files (too large)
*.bin
*.gguf
whisper_models/

# Audio temp files
*.wav
*.mp3

# Environment
.env
.env.bak*
secret.token

# Runtime artifacts
logs/
evidence/
benchmarks/
pids/
run/

# Backups and generated clutter
*.bak
*.bak_*
scripts/*.bak
scripts/*.bak_*
*.tar.gz
*.zip

# Embedded repos
chatterbox/
chatterbox-venv/
chroma_db.backup*/
*.onnx
.env
snapshot.json
tinytuya.json
tuya-raw.json

# Nested git repositories (third-party clones)
gesture-control/midiGesture-base/

# Artifacts - generated audit/test logs (keep only canonical refs)
artifacts/rag_audit_*.json
artifacts/gateBRAIN_*.log
artifacts/perf_probe_*.json
# Keep: artifacts/last_good_candidate_commit.txt (canonical)
# Keep: artifacts/rag_audit_*.md (forensic reports)

# RAG index caches (regeneratable)
rag/cache/
rag/*.cache
# Keep: rag/index_manifest.json (reproducibility reference)
# Keep: rag/rebuild_index_clean.py (canonical rebuild script)

# Credentials and secrets (NEVER COMMIT)
github.token
vault/creds/
vault/tokens/
*.token
*.key
*.pem

# Runtime data directories
content-pipeline/jobs/
content-pipeline/publish/
content-pipeline/production/
data/feedback/
forensics/
obs-portable/
obs-acceptance/
